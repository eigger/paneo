# Paneo Workspace Rules

- **Do not commit automatically**: Do not run `git commit`, `git push`, or create PRs automatically unless the user explicitly requests it. Only perform source code and documentation modifications, and let the user handle staging, committing, and pushing.
